{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd api && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --preload",
    "restartPolicyType": "ON_FAILURE"
  }
}
